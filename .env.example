# ============================================
# SPOTIFY MCP INTEGRATION - AGENT SDK EDITION
# ============================================
# This project uses Anthropic's Agent SDK for AI-powered music synchronization.
# See README.md for setup instructions.

# ============================================
# REQUIRED CONFIGURATION
# ============================================

# Anthropic API Key (REQUIRED for Agent SDK)
# Get from: https://console.anthropic.com/settings/keys
# This is the core AI engine that powers intelligent track matching
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Spotify API Credentials (REQUIRED)
# Get these from: https://developer.spotify.com/dashboard
# 1. Create a new app in the Spotify Developer Dashboard
# 2. Copy the Client ID and Client Secret
# 3. Add redirect URI: http://127.0.0.1:8888/callback
#    IMPORTANT: Spotify requires explicit loopback IP (127.0.0.1), NOT "localhost"
#    See: https://developer.spotify.com/documentation/web-api/concepts/redirect-uris
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIFY_REDIRECT_URI=http://127.0.0.1:8888/callback

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Default Playlist ID (optional, can be provided per-request)
# Get ID from Spotify playlist URL:
# Example: spotify.com/playlist/37i9dQZF1DXcBWIGoYBM5M
#          → Playlist ID is: 37i9dQZF1DXcBWIGoYBM5M
DEFAULT_PLAYLIST_ID=your_playlist_id_here

# Agent SDK Model (optional, defaults to latest Claude Sonnet)
# Options: claude-sonnet-4-5, claude-opus-4-5, etc.
# See: https://docs.anthropic.com/en/docs/about-claude/models
CLAUDE_MODEL=claude-sonnet-4-5

# API Server Configuration (optional)
API_HOST=0.0.0.0
API_PORT=8000

# MCP Server Configuration (optional)
# Timeout for MCP server startup in seconds
MCP_SERVER_TIMEOUT=60

# Logging (optional)
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ============================================
# DEPRECATED VARIABLES (No Longer Used)
# ============================================
# The following variables were used in previous architectures
# and are no longer needed with Agent SDK:
#
# - USE_TEMPORAL (Temporal mode removed)
# - TEMPORAL_HOST (Temporal removed)
# - TEMPORAL_NAMESPACE (Temporal removed)
# - AI_PROVIDER (Now always uses Agent SDK with Claude)
# - OPENAI_API_KEY (OpenAI provider removed)
# - AI_MODEL (Now uses CLAUDE_MODEL only)
# - FUZZY_MATCH_THRESHOLD (AI handles matching automatically)
# - USE_AI_DISAMBIGUATION (AI always used via Agent SDK)
# - MAX_CONCURRENT_ACTIVITIES (Worker removed)
# - MAX_CONCURRENT_WORKFLOWS (Worker removed)
# - MAX_ACTIVITIES_PER_SECOND (Worker removed)
#
# See MIGRATION_GUIDE.md for details on architecture changes.
