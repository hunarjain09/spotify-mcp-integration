# Firebase Functions Environment Variables
# Copy this to .env.yaml for local development

# These should be set as Firebase Secrets (not environment variables)
# Use: firebase functions:secrets:set SECRET_NAME

# ANTHROPIC_API_KEY - Set via: firebase functions:secrets:set ANTHROPIC_API_KEY
# SPOTIFY_CLIENT_ID - Set via: firebase functions:secrets:set SPOTIFY_CLIENT_ID
# SPOTIFY_CLIENT_SECRET - Set via: firebase functions:secrets:set SPOTIFY_CLIENT_SECRET
# DEFAULT_PLAYLIST_ID - Set via: firebase functions:secrets:set DEFAULT_PLAYLIST_ID

# Non-sensitive config (can use environment variables)
# SPOTIFY_REDIRECT_URI=http://127.0.0.1:8888/callback
# LOG_LEVEL=INFO
